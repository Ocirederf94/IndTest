This app provides two endpoints :
	api/v1/prices -> Returns all of ItemPrice properties
	api/v1/prices/data -> Returns only the required properties from ItemPrice 

For simple testing swagger is also provided by accessing /swagger-ui.html